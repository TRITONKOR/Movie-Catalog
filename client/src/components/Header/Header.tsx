import ProfileCard from '../ProfileCard/ProfileCard';

const Header = () => {
    return (
        <div>
            <p className="title"></p>
            <ProfileCard />
        </div>
    );
};

export default Header;
